
import { actions } from './actions'
import { getters } from './getters'
import { mutations } from './mutations'
import { state } from './state'
import { uploadBrainMapValidations } from './validations'
import { uploadStepsModel, uploadIdModel, classifyResponse, classifyWaiting, classifySend } from './model'

export {
  actions,
  getters,
  mutations,
  state,
  uploadBrainMapValidations,
  uploadStepsModel,
  uploadIdModel,
  classifyResponse,
  classifyWaiting,
  classifySend
}
